/**
 * DEPRECATED — no longer needed.
 * The project now uses Turso (@libsql/client) exclusively.
 * better-sqlite3 (local file SQLite) has been removed.
 */
console.log("ℹ️  ensureBetterSqlite is deprecated. The project uses Turso DB only.");
process.exit(0);
